# Django application environment variables
SECRET_KEY={{ lookup('env', 'SECRET_KEY') | default('django-insecure-default-key-for-development', true) }}
DEBUG=False
ALLOWED_HOSTS={{ domain_name }},www.{{ domain_name }},localhost,127.0.0.1

# Database configuration
DB_NAME={{ app_name }}
DB_USER={{ app_name }}_user
DB_PASSWORD={{ lookup('env', 'DB_PASSWORD') | default('postgres', true) }}
DB_HOST=localhost
DB_PORT=5432

# Email configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_HOST_USER=p.mutuku@alustudent.com
EMAIL_HOST_PASSWORD={{ lookup('env', 'EMAIL_PASSWORD') | default('nfic tvma epsj lssm', true) }}
DEFAULT_FROM_EMAIL=p.mutuku@alustudent.com

# Static and media files
STATIC_URL=/static/
MEDIA_URL=/media/